# Rust build artifacts
/target
**/*.rs.bk

# macOS
.DS_Store

# Editor
*.swp
*~
.idea/
.vscode/

# Environment
.env

# Android prebuilt vendor OpenCL stub (obtain per-device; see README)
/libs/
# Generated kernel concatenation
engine/kernels/kernels

# AI agent / internal working artifacts (keep out of the public repo)
.agent/
.claude/
**/handoff_*.md
**/HANDOFF*.md
*.handoff.md
.cursor/
# Local scratch / analysis notes
/experiments/

# Python
__pycache__/
*.pyc

# Local device/host config (commit only the .example templates)
/hosts.toml
/devices.toml

# Model weights (never commit — large; fetch per the demo/quickstart instructions)
*.gguf
*.safetensors
*.bin
/models/

# Profiling / debug output
perf.data
perf.data.*
*.folded
flamegraph.svg
core
core.*

# Standalone plugin / build outputs copied outside target/
*.so
*.dylib
